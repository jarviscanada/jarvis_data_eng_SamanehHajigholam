apiVersion: v1
kind: Service
metadata:
  name: trading-app
  labels:
    app: trading-app
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: trading-app
    tier: app-server
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: trading-app
  labels:
    app: trading-app
spec:
  selector:
    matchLabels:
      app: trading-app
      tier: app-server
  strategy:
    type: Recreate
  replicas: 2
  template:
    metadata:
      labels:
        app: trading-app
        tier: app-server
    spec:
      containers:
        - image: acrsamaneh.azurecr.io/trading-app:latest
          name: trading-app
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://trading-db:5432/jrvstrading
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: trading-app
                  key: user
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: trading-app
                  key: password
          ports:
            - containerPort: 8080
              name: trading-app
